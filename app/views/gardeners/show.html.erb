<div class="panel panel-default">
<strong>Home</strong>
</p>
<table class="table">
  <thead>
  <tr>
    <th> </th>
    <th> First Name </th>
    <th> Address</th>
    <th> Contact Number</th>

  </tr>
  </thead>
  <tbody>
      <tr>


        <td><%= image_tag @gardener.avatar.url(:thumb), :class => "img-circle" %></td>
        <td><%= @gardener.first_name %></td>
        <td><%= @gardener.address %></td>
        <td><%= @gardener.contact_number %></td>

        <td><%= link_to 'Edit', edit_group_gardener_path(@gardener.group, @gardener), class: "btn btn-lg btn-success" %></td>
        <td><%= link_to 'Begin Initial Questionnaire', new_group_gardener_build_path(@gardener.group, @gardener), class: "btn btn-lg btn-info" %></td>
      </tr>
  </tbody>
</table>

<%= link_to 'Back', groups_path, class: "btn btn-lg btn-danger" %> 

</div>
